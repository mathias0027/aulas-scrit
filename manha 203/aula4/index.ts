/*
import * as readlineSync from 'readline-sync';

let nome : string = readlineSync.questin(`qual é seu nome`)

let idade : number = readlineSync.question(`qual sua idade`)

let  temcarteira: boolean  = readlineSync.question(´'tem carteira?sim/nao´)


function podedirigir(nome: string, idade: number, temcarteira: string): void {
    if( idade >= 18 && temcarteira === 'sim'){
        console.log(`${nome} voce pode dirigir`)
    } else {
        console.log(`${nome} voce nao pode doirigir `)
    }
}
podedirigir(nome, idade , temcarteira)*/